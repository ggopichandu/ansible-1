- name: variables from inventory
  hosts: localhost
  tasks:
  - name: print information
    vars:
    timings: "8:00 PM IST"
  ansible.builtin.debug:
    msg: "Hi, I am learning {{Course}} from trainer {{Trainer}}, Duration is {{Duration}}.Timings are: {{timings}}"  